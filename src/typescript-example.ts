
/*const a:string = "";

function isEmpty(s:string):boolean {
  return s.length === 0;
}

let isnameEmpty:boolean = isEmpty(a);

interface IStudent {
  name:string
  age?: number
  class: number
}
class Student {
  name:string;
  constructor(constructorName:string){
    this.name = constructorName
  }

  printStudentDetails(student:IStudent, teacher?: string){
    console.log(student.name);
    console.log(student.age);
    console.log(student.class);
  }
}

const student: Student = new Student("Arun");
//console.log(student.name);
student.printStudentDetails({name:"arun",class:5});

*/